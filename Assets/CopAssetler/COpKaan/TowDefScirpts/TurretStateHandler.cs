using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurretStateHandler : MonoBehaviour
{

    public bool isInHand;
    
    // Start is called before the first frame update
    void Start()
    {
        isInHand = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
